<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>All Listings</title>
</head>
<body>
   
  <!-- Include the navbar partial -->
  <%-include('../partials/_navbar.ejs') %>

  <h1>Here's all the Listings</h1>
  <!-- Add a link to the new listing form -->
  <a href="/listings/new">Add a New Listing</a>
  
  <!-- Existing code for listing display -->
  <% if (listings.length > 0) { %>
    <ul>
      <% listings.forEach(listing => { %>
        <li>
          A listing in <%= listing.city %> owned by <%= listing.owner ? listing.owner.username : 'Unknown' %>.
          <a href="/listings/<%= listing._id %>">View more details.</a>
        </li>
      <% }) %>
    </ul>
  <% } else { %>
    <p>No listings available.</p>
  <% } %>
  <input type="hidden" name="_csrf" value="<%= csrfToken %>">
</body>
</html>


